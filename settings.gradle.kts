pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "SmartGlass-AI-Agent"
include(":sdk-android")
include(":sample")
