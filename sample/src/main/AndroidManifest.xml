<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartGlassSample">
        <!-- ComposeActivity with modern Jetpack Compose UI (new default) -->
        <activity
            android:name="com.smartglass.sample.ComposeActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartGlassSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- SampleActivity with XML-based UI (kept for backward compatibility) -->
        <activity
            android:name="com.smartglass.sample.SampleActivity"
            android:exported="false" />
        
        <activity
            android:name="com.smartglass.sample.PrivacySettingsActivity"
            android:label="@string/privacy_settings"
            android:parentActivityName="com.smartglass.sample.ComposeActivity" />
    </application>

</manifest>
